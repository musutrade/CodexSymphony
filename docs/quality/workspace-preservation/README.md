# 第二阶段前收尾记录

后续状态：#57 已双 Gate 通过、合并并部署，见 [安装回读](installed-pr57.json)。
删除保护已启用。安装后核对发现 Agent 无法访问宿主归档器，后续修正将交接前归档
交给宿主 after_run；[该工作流摘要](host-handoff-review.json) 待重新审定。

## #56 产品部署

已部署合并提交 `5ab4dc26630c05625d26c390df0db12bea457331`。使用独立发布目录和
systemd drop-in，旧部署仍保留；现有数据库、Runtime/GitHub/存储配置保持原路径。
[部署摘要](deployment.json) 记录二进制和原始观测的摘要及保留位置。

约 122 秒、25 次采样：两仓 delivery_ready、repository_ready、runtime_ready 均为 true，
第二仓 PR 观察未出现 stale。部署前后的 owner、模型调用和预算授权逐项一致。

发现已有 storage_guard 故障锁；只读核查没有容量或期限超限。通过当前对象版本调用
既有 storage_recheck 接口，版本更新为 5，保护锁解除且 recovery_complete=true。
第一仓/第二仓模型调用仍为 5/1，owner 仍为 2，需求仍处于 Submitted。
首次使用不匹配 Origin 的请求返回 403、无副作用，改为实际同源后成功。
此操作没有合并产品测试仓 PR，也不构成新需求验收。

## 工作区保全修复

实现和部署契约见 [证据保全](../../workspace-preservation.md)。

- Python：36 项测试通过，含 7 项保全故障测试及安装器版本核对。
- 控制器 `make all`：358 项测试、0 失败、6 跳过；格式、规格检查、Credo、覆盖率和 Dialyzer 通过。
- 真实控制器与 Python 归档器联动：缺失/错误证据时保留目录，配置存储重启后仍执行约束，
  正确归档后删除合成工作区，归档原件保留。没有删除历史工作区或历史证据。
- #57 提交时仅完成代码与测试，尚未启用活动工作流的新保全钩子；当时待受保护输入重新审定、
  精确提交双 Gate 通过后安装并重启开发控制器。

这项修复防止后续同类缺口，不证明 GH-25 原件一定由控制器删除；没有逐文件删除记录。
历史 A13 剩余四项原件仍 unresolved，不能将新观测冒充旧原件或勾选完整发布验收。
