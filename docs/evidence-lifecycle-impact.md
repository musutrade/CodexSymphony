# 证据生命周期：实践复盘与需求影响

核对日期：2026-09-16。产品代码基线：`cba6a54475209405b64ee6bca0720444cd054be6`
（GH-18 / PR #39 合并）。GitHub 中 GH-12～18 已关闭；GH-19～25 尚未关闭。
状态仅是本次核对快照；Issue 关闭不意味着后来新增的验收已通过。

## 结论与规范来源

已开发内容涉及工作保全、事件持久化、磁盘保护和累计模型额度，证据生命周期尚未形成产品闭环。
沿用现有实现、增量补齐接口和验收，不要求重做或重新打开所有已关闭任务。
权威行为定义在[主方案 10.4–10.6](../Personal_AI_Software_Factory_综合方案.md#104-证据分级与保留)；
本文件只记录实施现状和归属，不定义另一套参数或保留期限。

旧主方案及 GH-23 明确要求“首版不自动 GC 保全副本和验证证据”，会持续积累后以容量不足暂停。
本次修订取消这一默认永久保留规则，但不取消唯一原件、活动恢复消费者或未对账 partial/pending 的保护。

## 已开发需求：可复用的部分与缺口

| 需求 | 已有实现与证据位置 | 本次影响与增量工作 |
|---|---|---|
| GH-13：需求/不可变策略 | `apps/server/src/business.rs`、`migrations/0001_requirements.sql` | 保留版本化/授权基础；存储策略版本及适用范围仍需接入，不把既有 gate 策略误称为存储策略 |
| GH-14：执行控制/恢复闸门 | `apps/server/src/run_store.rs`、`coordinator.rs`；`run_event` 持久化 | 复用旧进程静止证明、唯一 Run 和恢复屏障；清理需与消费者领取互斥。事件原始 payload 尚无完整生命周期约束 |
| GH-15：工作区/Broker | `git_broker.rs`、`workspace_files.rs`、`workspace_store.rs`；`migrations/0003_workspaces.sql` | 已保全未提交/未跟踪/未推送工作并排除根部 target/node_modules/.angular；补资料大小、保护/消费者状态和解除保护后的回收。不能删除仍是唯一实现的历史文件 |
| GH-17：预检/磁盘保护 | `storage.rs`、`preparation.rs`、`preparation_store.rs`；`migrations/0005_preparation.sql` | 已有实际余量、写入/fsync/数据库持久性检查、故障锁存和停止链；当前余量阈值不是需求/类别预算，也没有在途字节预留和自动回收。增量连接存储准入，不绕过现有故障保护 |
| GH-18：累计额度 | `budget.rs`、`budget_store.rs`；`migrations/0006_budget.sql` | token/turn/model_seconds 的预留与去重可作为设计参考；尚无磁盘字节额度，不能宣称“已有预算所以存储也受限”。存储分配/占用/回收另行记账，不修改既有费用事实 |

这些已关闭任务按当时范围完成仍然成立。新的 A10 不能回填成既有通过记录；需要补齐后重新执行受影响的恢复、磁盘和累计约束回归。

## 后续需求的承接范围

| 需求 | 需要承接的内容 |
|---|---|
| GH-19：Runtime（尚未合并） | 标识原始协议/日志的所属 Run 和类型，流式大小/数量约束及截断标识；不能因新增回收规则影响仍待回答的问题与恢复状态 |
| GH-20：固定候选验证 | 生产证据时登记 SHA/配置/工具/阶段、大小、摘要与消费者；结果与原始材料可用性分离，缺失或过期不能冒充新验证；验证重试生成可归并的明确关系 |
| GH-21：可靠交接 | 保存精确 repo/PR/head/attempt 关联和当前交接消费者；确认后续成功确实替代旧尝试，不能用旧 PASS 清掉新失败，也不混同 post-merge 阶段 |
| GH-22：页面/指标 | 展示可用/压缩/过期、占用/预留/受保护字节、清理失败和容量阻塞；不另建监控平台 |
| **GH-23：生命周期闭环主责** | 修订旧“72 小时完整保留、证据不 GC”范围；实现分类、存储预留/实际用量、准备/失败/成功/取消残留登记、重试归并、TTL/总量清理及中断对账。增量接入 GH-15/17/18 与 GH-19～22，不推倒重写 |
| GH-24：集成验收 | 复用前序测试，增加连续多批需求和多次重试、跨保留窗口、后台清理并存、清理中断/重启、唯一工作保护、数据库/日志占用和不足时安全拒绝新工作；测实际容量，不只断言调用了删除函数 |
| GH-25：第二仓验收 | 证明两个仓库共享全局预算而归属清理互不越界；仍保持严格串行 |

保持原串行依赖顺序。GH-23 对前序生产者的必要增量接入属于该闭环范围；在其之前，前序阶段通过不能声称产品已满足完整存储生命周期验收。首条 PR 可按既定路径贯通，0a 发布必须通过修订后的 A10。

## 任务文本与实施状态

本次是规范与影响分析变更，没有实现 Rust 产品的回收器、字节额度或数据库清理，也没有修改 GitHub Issue 的正文、状态、标签或调度器配置。
上述 Issue 仍引用旧版固定提交，尤其 GH-23 的范围与新主方案直接冲突；这份规范获采纳并进入主分支后，须同步 GH-19～25 的实施引用和相关范围/验收，保留原依赖和已完成事实。不能让 Agent 自行猜测旧 Issue 与新方案的优先级。

PR #40 的 Python/systemd 脚本属于开发宿主：已实践二进制短期保留、复盘压缩、冷归档限额，以及同 PR 成功后的失败尝试归并和 disposable clone 清理。它们提供故障样本与测试思路，不能直接拿固定宿主路径、GitHub CLI 查询或开发机器 4 GiB 参数代替产品内部身份、持久化授权和存储准入。

## 验收补充检查表

- 多次失败后通过：保留成功复盘记录、早期原因/日志/身份，早期大文件和准备 clone 不遗漏。
- 失败后尚未成功、成功后的新失败、其他修订/PR、post-merge 检查：不误归并。
- 已推送可重建提交与未推送/脏工作分别处理；磁盘超额也不能删除唯一工作。
- 无最终报告、准备中断、partial/pending：可登记/可诊断，原件受保护，未知身份不猜测删除。
- 压缩校验后中断、删除中断、数据库更新失败：原复盘包/摘要不被残缺的新包覆盖，可幂等续做。
- TTL、单条/Run/需求累计/类别/全局预算、在途预留、控制面余量、后台归档峰值共同参与容量验收。
- 长期摘要和数据库记录也计入预算；不能用删文件来掩盖无限增长的事件表或 WAL。
- 磁盘挂载缺失、目录身份变化、符号链接及嵌套挂载：阻止越界/写回系统盘；不调用模型清理。
