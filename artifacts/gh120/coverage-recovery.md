# GH-120 coverage recovery

The complete export for Gate run-98a73620b935 is now available in host-quality-run-98a73620b935/. Export hashes were verified against evidence.json. The host matched all 693 source inventory entries and reported exactly four coverage failures on three functions: validation_service::accepted, validation_supervisor::settle and validation_context::decode_retained. The prior blocked declarations and reports remain historical evidence.

The existing real Git/PostgreSQL/process integration now checks that accepted validation with missing binding evidence fails without regenerating it; missing or malformed context/evaluation/approved-plan records reject delivery; and recovery without quiescence retains the original stop intent, does not re-execute the Hook, and cannot admit even when the original quiescence proof subsequently returns. Malformed quiescence proof is also rejected. Controlled fixture restoration is test setup only and is not a product recovery procedure. No production logic, policy, coverage threshold or CRAP threshold changed during this recovery.

Current commands and exit statuses are in coverage-recovery-checks.json. The full suite uses /opt/symphony-env/workspace_tests.py with the disposable PostgreSQL fixture and serial Rust scheduling, including the real pinned Runtime integration. The initial targeted smoke and subsequent behavioral regression both passed. No GitHub/cloud/notification side-effect spike ran. Environment provisioning is captured in the full suite output; writable build/temp paths and installed tools were checked. The dbctl status invocation initially omitted its required role, then status test and status dev both confirmed running fixtures with their approved resource limits.

source-inventory-coverage-recovery.json binds the current source paths, hashes and Git executable modes. Normal-path-quality-fixed/ retains the actual process/Git/PostgreSQL validation evidence for unchanged production code, with its raw-reference retention map. The new tests change only fixture fault injection and assertions; they do not fabricate external validation or formal signatures. Migration 0033, access-path audit and compatible rollback remain documented in docs/validation-hooks.md.

The evidence manifest is .symphony-evidence.json. The complete exact-tree local_gate must run after all source and evidence edits are finished; these ordinary checks do not assert a complete Gate PASS or protected CI acceptance. Publication, if permitted by the complete Gate, must use the host-returned SHA and verified remote tree. No after_run archive-copy success is claimed. The remaining serial delivery work stays with #121, #105 and #122.
