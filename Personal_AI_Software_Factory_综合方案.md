# Personal AI Software Factory 综合方案

> V10.4 Draft · 2026-09-16 · Current implementation: Phase 0a；日常 V1 范围已确认
> 本文是当前实施契约。第 23 章定义实施队列，第 24 章引用验收场景；两章不另行定义行为。

## 0. 结论摘要

### 0.1 目标与当前边界

个人使用：评审需求后，由 Agent 编码，平台负责验证、PR、CI 和交接，正常路径减少人工介入。
首个日常可用版本是 Agent → PR → CI → Fix → Merge → 业务验收完成；0a 的 localhost 需求到 PR 仅为内部里程碑。
0a 只接管本人可信仓库，不提供多租户、远程执行或不可信代码隔离承诺。

先跑通一条单仓真实需求，再在 0a 尾部增加多仓登记；始终严格全局串行，具体释放条件见第 6 章。
[日常 V1 契约](docs/daily-use-v1.md)明确电脑/手机/Agent 草稿入口、大需求拆分、整组评审、严格串行自动交付及手机接续，均为首个日常版本必需项。0a 的 Submitted 不是 Done。

### 0.2 规范效力与唯一来源

- [架构边界](docs/architecture-boundaries.md)只定义三个真相及 Gate 的职责边界。
- 本文第 1～22、28 章定义当前行为；第 23/24 章只引用这些定义，不增加隐含要求。
- [日常 V1 契约](docs/daily-use-v1.md)是已确认的后续交付要求，定义 M1–M3 与 B01–B08；不反向扩大内部 0a 的 A01–A13。
- [演进目录](docs/roadmap-specs/README.md)是非规范性候选方案，不是当前实施、建表或验收前置条件。
- spike 是锁定版本的实验记录，复盘是历史证据；其中旧版“必须”和旧章节引用不再产生当前约束。
- 同一规则只保留一个权威定义；发现冲突时修正来源，不要求实现者自行从终态规格推导删减。
- [V9 原文](https://github.com/musutrade/CodexSymphony/blob/05d7cade41054960f688ab409744826a4be87da7/Personal_AI_Software_Factory_综合方案.md)仅供历史追溯，不作为当前规范。

### 0.3 P0 判据

能力只有直接实现当前用户流程，或守住当前数据、授权、执行和交接不变量，且没有更简单的实现方式，才进入 P0。
先确定行为和不变量，再形成测试；不能用既有验收条目反向证明机制必须保留。
真实事故是支持证据，不是唯一来源。条目数、文档行数和预估节省周数均不是范围判据。

## 1. 产品定位

用户创建可机器验证的需求，评审后进入队列。正常执行不重复确认；提问、授权不足、未知故障或预算耗尽时显示可操作待办。
电脑与手机共享需求入口，Agent 草稿可粘贴/导入为 Draft。父子需求与整组授权的 V1 行为仅由日常 V1 契约定义。
当前 0a 四个页面是需求表单、需求列表、需求详情（含 Run 时间线）和待办箱。
GitHub 是代码、PR、CI 的外部事实来源；GitHub 上合并的事实本身不代表业务验收完成。

## 2. 从 Symphony 继承什么

保留独立工作区、单协调者、执行超时、持久化恢复和外部对账。
用 Rust 实现控制面，复用 Codex app-server 协议，不提前实现多 Runtime 抽象框架。
等待 CI、网络重试、PR 查询和资源清理由普通程序执行，不占用模型会话。

## 3. 核心领域模型

### 3.1 最小记录

| 记录 | 当前职责 |
|---|---|
| Project / Repository | 项目归属、精确远端身份、基线分支和仓库策略 |
| Requirement / RequirementRevision | 业务状态与不可变需求输入 |
| AgentRun | 一次执行的状态、进程身份、会话、工作区、输入快照和消耗 |
| ValidationEvidence | 指定源码和受信验证配置的结果、摘要与材料引用；记录材料可用/已压缩/已过期，不混同历史结果 |
| Completion / Candidate | Agent 结束声明与平台保全的不可变提交 |
| HandoffOperation / Outbox | 发布意图、幂等键、实际结果及下次重试时间 |
| GitHubObservation | 精确 repo/PR/head、Checks 摘要、合并或关闭事实及观察时间 |
| RuntimeRequest / Blocker | 待回答问题、阻塞原因、解除条件、进度与恢复位置 |

逻辑记录可内嵌 JSONB，不要求每个名词独立建表。需求、执行、外部事实仍可独立查询。
恢复工作副本见第 10 章；最小诊断字段见第 18 章；不建立诊断 Agent 或通用 action catalog。

### 3.2 策略归属

`gate_recovery_policy` 只在 Repository 配置；0a 不支持 Requirement 级覆盖。
需求执行获授权时保存实际采用的不可变策略快照，Run 和恢复操作引用该快照。
策略更新用于后续授权，不能静默改变当前尝试的权限或重置累计预算；安全撤销立即阻止后续动作。
重新授权保存新版本和原因，历史消耗保留；0a 不提供增加代码修复次数的入口。

## 4. 状态与控制命令

### 4.1 Requirement

| 状态/动作 | 语义 |
|---|---|
| Draft → Ready | Contract 校验成功且用户评审通过；不再要求 Start |
| Ready → Running | 第 6 章领取条件满足 |
| Running → Submitted | 第 11 章交接成功并持久化 PR 关联 |
| Running → Failed | 自动恢复不可用、未知错误或预算耗尽；保留失败阶段与待办 |
| Draft / Ready / Running / Failed / Submitted → Cancelled | 持久化取消意图；4.4 的收尾完成前不释放队列 |
| Failed → Running | 用户恢复或已授权条件恢复，通过第 10 章检查后从记录阶段继续 |

人工问题待定时保持 Running，使用持久化请求和等待原因表达，不新增审批状态机。
Submitted 保存外部合并/关闭观察，0a 不自动置 Done。取消与暂停的动作以 4.4 为准，队列释放以第 6 章为准。

### 4.2 AgentRun

Run 使用 Created / Running / Succeeded / Failed / Interrupted。成功表示有效声明、执行组静止及候选保全，不表示验证通过。
验证失败新增独立证据，不改写已终结的 Run。恢复需要编码时新建 Run 并引用原 Run；仅验证或交接无需新 Run。

### 4.3 输入修改

Draft 可编辑；Ready 可撤回 Draft。Running/Submitted 拒绝普通 PATCH 修改输入。
0a 不实现执行中 revise 和全量输入漂移自动重验证：先停止、保全并核对外部动作，再创建引用原任务的新 Draft；新输入重新评审。
回答问题不能隐式改变 Contract 或扩大权限。权限扩大按第 16 章处理。

### 4.4 暂停、恢复与取消收尾

暂停先保存控制意图，阻止该任务新编码/验证/发布/合并动作，停止现有本地执行组并保全工作；在途 GitHub 请求只对账，不假装撤回。
已领取任务暂停后仍占全局队列，环境探测成功或业务问题回答不自动解除用户暂停；用户恢复后从已保存阶段继续。
尚未领取的项暂停后不被领取；队列顺序不自动跳过它。暂停全局队列采用同样规则，不新增业务状态机。

“取消并终止交付”一次操作授权该任务的明确收尾：保存取消意图 → 停止并保全 → 对账未知发布/合并结果 → 幂等关闭归属已核实且仍开放的关联 PR → 确认结果后释放。
关闭 PR 的意图经 outbox 先落库；不删除分支/提交，也不关闭其他任务的 PR。已经合并的 PR 保留真实合并记录，不尝试关闭或自动回滚。
取消与合并竞态时先确认最终事实，再记录取消生效范围；有未确认外部结果/未保全工作时不释放，失败在同一待办显示具体收尾动作并有限重试。
取消不等于 Done，依赖被取消项的任务不能自动执行；其他后继仍按既定顺序和各自依赖检查领取。
取消后的发布禁止规则不禁止已授权的停止、保全、观察和关闭 PR 收尾。重复取消不重复生成动作。

## 5. 总体架构

一个 Rust 控制面进程 + PostgreSQL + Angular。控制面内包含 API、一个协调 tick、子进程监督和 outbox 发送任务。
模型执行、日志读取和网络调用异步运行；协调 tick 不等待长调用，不因一个查询超时阻塞状态 API。
进程监督负责启动/停止事实；tick 依据持久化状态选择准备、恢复、验证或交接动作。
不建设 Event Bus、五套独立 Reconciler 或独立消息服务。重试按第 28 章退避，不在每次 tick 重复执行相同动作。

## 6. 严格全局串行与启动恢复

### 6.1 队列占用

数据库保存唯一的当前队列占用者 Requirement；领取与状态更新同事务执行，以唯一约束/条件更新防止重复领取。
并发限制覆盖整条需求，而非只统计活跃 Agent。编码结束、验证、交接重试、等待 CI、人工阻塞均不自动释放队列。
0a 在 PR 已由 GitHub 确认合并后允许释放；仅关闭但未合并的 PR 需用户明确取消/放弃任务。
合并事实用于队列释放，不自动证明业务 Done。没有远端 PR 的失败任务也需恢复或明确取消，不静默跳过。
取消执行 4.4 收尾，确认所有关联 PR 的合并/关闭与本地保全后才释放；暂停不释放。
异步清理不占队列，但磁盘不足会暂停新领取。

这条规则承担 0a 的分支占用：交接未终结、外部结果未知时不发放下一项执行权；无需 `worktree_leases`。
多仓登记后仍沿用同一全局占用者，不因切换仓库绕过顺序。日常 V1 的 code_change / validation_only 完成条件及当前项内修复见日常 V1 第 3、5 节；0a 合并即释放规则不替代 V1 验收。

### 6.2 冷启动

- 取得主机单实例锁后才能启动协调器；部署仅允许一个控制面实例，不支持跨主机副本。
- 启动前持久化 Run、工作区、启动意图和本次进程 incarnation；启动后记录进程组标识及启动时间，防 PID 重用误认。
- 重启先确认旧执行组与全部写入子进程已静止；身份不明则阻塞，不能仅凭数据库无 Running 行就重跑。
- 保全可读取工作，检查未完成验证与 outbox；未知外部结果先查询 GitHub。
- 第 10 章恢复检查完成后，才继续原任务或释放队列。迟到消息核对 Run/request/incarnation，只归档不推进当前状态。

不建双租约、dispatch_epoch、Repository lease、固定零值占位字段或分布式看门狗。
单实例锁、Run 身份、当前输入版本与数据库 CAS 各承担不同检查，不能用唯一索引替代进程静止确认。

## 7. Requirement Contract

当前必填为 title、description、acceptance_criteria、validation_plan；network_access 可空，仅表达联网意图。
平台生成 revision、AC ID、创建者和时间，无需用户填写长期目标模型的全部字段。

每条 AC 包含 ID、描述及 verification_ref；引用一个验证步骤或受信检查。步骤记录命令/检查选择器、预期结果和超时。
引用可以指向本需求将新增的测试；Ready 检查计划可解析、可执行的方式明确，不要求待开发测试此时已经存在。
验证命令及受保护入口由仓库策略约束；需求文本不能作为无审查的宿主机 shell。
Ready 冻结 Contract revision 与策略快照。0a 不接受默认人工验收或 Agent 自报 N/A 代替必需 AC 证据。
ADR/OpenSpec 可作为上下文引用，不新增同步状态或强制文档链。执行中输入修改见 4.3。

## 8. Codex Runtime

每个 Run 启动独立 app-server，进程 cwd 和 thread cwd 都是该 Run 的 worktree。
采用 Symphony 的可信开发环境方式：开发命令、构建、数据库/浏览器与真实 Runtime 集成测试在同一准备好的环境内正常执行。
Runtime 使用 approval_policy=never、danger-full-access；不叠加逐命令沙箱，不要求嵌套 namespace，也不为不同测试安装宿主专用入口或逐源码 manifest。
部署环境应为本人可信项目准备，GitHub 和 Gate 签名凭据留在独立服务。部署可使用专用账号或单个环境边界，但不把这些部署选择变成产品内测试协议。
子进程不继承控制面凭据；项目工具与依赖在部署准备阶段配置。
锁定经过验证的版本，从其 schema 生成所用协议类型；升级需重跑握手、工具、token、停止与真实开发命令用例。
S1 记录的版本是历史实验依据，不宣称对未来版本自动兼容。

实现 initialize/initialized、thread/start、turn/start、事件流及 turn/interrupt；stdout 协议和 stderr 诊断分开。
注册 `create_local_commit`、`report_completion`、`report_blocker` 三个动态工具，按原 request ID 回复。
工具参数由平台验证；重复请求返回已保存结果。完成与 blocker 竞争时，事务只接受一个结束意图，落库成功后才回复。
结束意图受理后停止续轮并确认执行组退出；工具回执或 turn/completed 均不能替代退出确认。
普通 turn 结束未声明完成时，仅在授权有效、无阻塞且预算未耗尽时续轮。

可信开发环境中的普通操作自动执行。意外的权限 RPC 作为配置不匹配处理；GitHub 外部动作仍须平台授权，不由开发命令持有凭据。
人工问题按 8.1 持久化并展示。模型进程等待最多 2 小时、单 Run 绝对寿命最多 8 小时，重启不重置；这些是进程退出/工作保全期限，不是业务问题的可回答期限。
startup/response/stall 超时按部署配置执行，停止后保全工作。提问等待不等同于代码失败，不消耗修复次数。
不实现 read_observation 动态分页、模型诊断、主动压缩或共享原生 TUI 控制连接。

### 8.1 持久化业务问题与隔夜回答

业务问题保存 requirement/revision、question_id/version、原 Run/request 关联、待答原因和答案；Runtime RPC 只是一次传输，不拥有业务问题的寿命。
进程等待超时或 Run 退出后停止保全，原 RPC 不再使用，问题仍待答；不自动让模型重新提问，也不消耗一次代码修复。
手机/电脑打开当前任务可回答最新仍有效的问题。平台校验需求授权、问题版本与未回答状态，CAS 保存答案并记录恢复意图；旧 RPC 迟到回包不能推进已结束 Run。
原会话仍有效则回复原请求，否则在旧组静止、工作保存且预算允许后创建关联恢复 Run，将已确认答案加入上下文，无需用户再点一次恢复或再次回答。
答案只解决该问题，不跳过其他 blocker、用户暂停、取消、安全撤销或待批准的输入变化；这些条件不满足时保留答案并显示剩余阻塞。
输入修订使问题失效、问题已取消/回答或版本不符时拒绝旧回答并返回当前状态；单纯进程退出不使业务问题失效。
原始问题、答案和恢复来源保留可追溯，累计额度按第 15 章执行。

## 9. 配置

部署 ENV 管数据库、路径、代理和 app-server 可执行文件；Repository 管 prompt、固定预检、验证入口及恢复策略。
运行使用已校验快照并记录身份；无效新配置不替换有效配置，无法确认授权有效性时暂停新动作。
WORKFLOW.md 若被使用，记录其 blob 身份；当前 Run 继续使用获授权的快照，不追踪任意上下文文件的全量漂移。
Agent 对策略/验证入口的修改只作为提案，不能改变本次受信检查。撤销与身份失配按第 17 章处理。

## 10. 工作区、保全与恢复

### 10.1 工作区与提交

canonical clone 和 Run worktree 分离；目录带唯一 Run ID，不复用旧路径。平台创建 `ai/req-*` 分支，记录基线 SHA。
Broker 用固定 Git 可执行文件和参数数组，禁用仓库 hooks/credential helper，不执行 Agent 提供的 Git shell。
Broker 提交前校验工作区归属、当前输入授权和写入状态；Agent 不直接写 Git 远端。

### 10.2 最小工作保全

停止旧执行组后，保留 HEAD、可达中间提交、index 与工作树差异、未跟踪源码/测试/进度文件，包括二进制和文件模式。
可重建构建缓存按明确规则排除并记录；不因超限静默丢掉实现。快照清单保留路径、摘要、完整性和存储引用。
不跟随链接越界，不复制凭证/hooks/任意 Git 配置。恢复到新 worktree，核对内容；不隐式 reset/rebase 到最新 main。
保存失败标记 partial/pending，保留唯一原目录、停止清理和自动重跑。摘要说明失败可以降级，工作副本丢失不能假装可恢复。

### 10.3 恢复选择顺序

核对暂停/取消/授权、旧组静止、阻塞解除和输入版本后，只选择当前恢复阶段仍有效的产物：待交接且验证有效则恢复交接；待验证候选恢复验证；仅有声明先核对并保全；否则从已保存工作新建编码 Run。
已有失败证据、已作废的候选或历史已合并 PR 不会因为“仍有 manifest”被重复交接；日常 V1 的合并后/集成修复按其第 5 节更新当前修复产物，历史记录不覆盖。
每次恢复记录来源、失败/解决证据、恢复阶段和预算消耗；不要求单独 RecoveryPlan 状态机。
未知根因由用户处理；固定可判定条件可在已授权范围内有限探测，满足后自动继续。探测不得回答待定人工问题或绕过安全撤销。

### 10.4 证据分级与保留

长期保留足以解释决策和定位代码的材料；完整保全仍承担恢复作用的唯一工作。证据不是所有运行目录的永久副本。源码已推送并核对精确提交后可从 Git 重建；GitHub 不替代未提交文件、未推送提交、原 index 或外部环境。重新编译产生新的验证结果，不声称逐字节重现历史二进制。

| 类别 | 最小内容 | 保留与回收条件 |
|---|---|---|
| 决策与交付记录 | Requirement/revision、Run/attempt、候选/基线 SHA、受信配置和工具身份、命令/输入摘要、结果与退出状态、批准和实际 PR/合并事实 | 压缩且单条限额；长期可查询，仍计入总容量和记录保留期限 |
| 复盘材料 | 脱敏失败原因、阶段、关键日志、检查摘要、完整性清单及来源引用 | 有限期限/大小；报告明细可压缩，重复重试按 10.5 归并；日志截断必须标记范围和原因，不把截断误作完整原文 |
| 恢复材料 | 10.2 的未交付工作、未推送提交、index/工作差异、当前恢复检查点 | 活动消费者或唯一副本不自动删除；恢复/交接完成且核实已不再依赖该副本后，才按期限回收 |
| 可重建产物 | target、依赖副本、测试二进制、原始覆盖率对象及临时资源 | 有限热保留窗口和字节上限；不默认转成永久冷归档，失败与成功运行适用同样的容量约束 |

每项资料保存所属身份、类别、创建时间、实际字节、摘要/存储引用、保留策略版本、到期时间或保护原因。使用现有记录和简单清单实现，不要求每个名词独立建表。未分类资料不得静默丢弃：纳入占用，阻止超额新领取并显示分类/保全待办。

完整复盘包先完成写入、可读/逐文件校验与引用持久化，才能移除冗余材料；中断后依据原清单继续，不能用缺文件的新快照覆盖原复盘包。删除留下原身份/摘要、时间、原因和替代记录。材料状态与结果状态分离：过期不把历史 PASS 改成 FAIL，也不能把仅有历史 PASS 当成新的候选已验证；仍待验收/交接消费的必需材料不可提前过期。

### 10.5 重试归并与周期回收

归并身份至少包含精确仓库、Requirement/revision、候选 SHA、验证阶段及策略身份；PR 只是关联字段。保存从失败尝试到后续解决尝试的明确关系。不同 PR、不同需求修订、成功后的新失败和合并后验证不能因路径或分支名相同而合并。

后续候选成功并完成适用消费者交接后，保留当前成功的完整复盘记录；被明确替代的早期失败或重复成功尝试保留原因、关键日志、输入/提交/策略身份和解决引用，回收可重建副本。未成功、取消、准备阶段失败、尚未创建验证目录的尝试也必须进入登记和清理流程；不能因缺少最终报告永远漏扫。无法证明归属/替代关系时仅执行已获准的普通期限清理，保留待办，不猜测成功。

每 5 分钟扫描已登记的唯一 Run 目录、准备目录及材料清单，阶段结束也触发检查。成功产物不再一律强制完整保留 72 小时；按 10.4 类别、实际消费者和 10.6 配额决定。冷归档也必须有到期和总量限制，不能把增长问题搬到另一块盘。

清理与消费者领取互斥核对身份及当前使用状态；清理期间该目录不可再分配，新任务使用新 ID。不跟随符号链接，不跨嵌套挂载，重复删除仅指向原唯一实例。活动进程、身份未知、仍待恢复/验收/交接的资料，以及未对账的 partial/pending 和唯一原件均受保护；它们照常计入容量，超额时停止新工作而非突破配额或丢原件。

清理失败保留记录，按第 28 章有限重试并聚合提示，不启动模型、不改写交接事实。扫描负责漏收和中断恢复，清理不占执行槽。测试临时资源归入 Run 私有目录，测试入口负责其子进程；未接入的宿主资源不声称自动回收。无需 ManagedResource、通用引用计数、清理 outbox 或独立资源平台。

### 10.6 存储预算与准入

部署必须配置有限的全局受管存储预算、控制面保留空间，以及工作区/缓存、热证据、冷归档、数据库/事件日志、长期复盘记录的类别预算和保留期限；同时配置单 Run 输出上限与需求跨重试的累计存储分配上限。安装检查拒绝缺失或无限配置。允许单机配置与现有页面展示，不引入计费/资源平台。

每次准备、领取、续轮或大型产物生成前，检查实际占用＋在途预留＋本次预留，以及所在文件系统实际余量。严格串行仍须计入后台归档、恢复副本和未完成清理。预留与调用意图持久化；写入结算为实际用量，中断后对账，未知结果不提前释放。需求重试和关联修订不重置累计分配事实；回收可释放已核实的在用字节，不返还已消耗的累计写入额度。存储统计与第 15 章模型额度分开，使用相同的幂等/对账原则。

平台写入的日志、附件和事件在入口限制大小与数量；原始流支持明确截断/轮转，关键状态写入失败按 12.3 停止外部动作。运行中检查实际增长，超过预算先停止新增可重建输出并回收已到期材料；仍不足则阻止领取/续轮并安全停止、保全。不得牺牲唯一工作来制造可用空间。声明 native 构建的硬字节隔离必须有经验证的文件系统/执行适配器支持；仅轮询余量不能宣传为任意进程写入的硬配额。

归档目标挂载与身份必须验证，缺失时不写回系统盘替代目录。控制面保留空间不可被任务分配，用于持久化停止、失败与清理结果。数据库行、索引、日志/WAL 和备份占用也要监测，不能只统计文件证据；未能安全到期回收的关键记录到达预算时，暂停并提示导出/扩容或明确调整策略，不无限增长、不静默删除历史授权和计费事实。

已有受保护副本纳入升级盘点，不因启用新规则自动失去保护。存储策略修改记录版本、作用范围和原因；降低额度先影响新准入，不追溯删除唯一工作，不重置累计事实。开发宿主实践的“最近两次、24 小时、4 GiB”等是该环境的参数样例，不是产品所有部署的固定值。

## 11. GitHub 交接

沿用 S2 已验证的 GitHub App，控制面保管私钥及 installation token，按目标仓库获取权限；不新增 PAT 路径。
代码凭证不放入 Agent 环境或工作区，0a 的读隔离限制见第 17 章。

验证通过后同事务保存产物清单与 outbox 意图。发送器条件 push（仅 fast-forward）、按 repo+branch 查询/创建 PR、保存关联，再进入 Submitted。
每个动作的稳定 action_key 绑定任务、输入版本、目标仓库/分支和候选 SHA；重复投递不重复发布。
push/PR 创建响应丢失时先读取远端事实，未知状态不强推、不换分支重建 PR、不重新启动编码。
冲突或身份不匹配转为可处理阻塞。取消后的迟到结果仍归档对账，不启动新的发布动作；4.4 的关闭 PR 收尾使用独立动作类型。

每 60 秒只读轮询已关联 PR：repo/PR/head/base、Checks 摘要、合并/关闭、last_synced_at 与 stale。
0a 不自动合并，不因观察到 CI 失败就创建修复需求；只展示结果和链接。查询失败退避，不调用模型。
无 webhook、签名验签/inbox 或 SSE 交付要求。队列释放引用第 6 章。

### 11.1 仓库交付能力与 CI 契约

仓库启用自动队列前校验所选阶段的实际能力；0a 校验准备/交接/只读观察，V1 启用前再确认完整 CI 恢复、自动合并与合并后验收路径。
契约记录精确仓库/默认分支、所需 App 权限与可执行动作、必需检查/适用性、触发方式、等待期限和受信来源，随仓库策略版本保存。
私有仓也验证读取 PR/Checks/所需日志、发布和所选 CI 恢复动作的授权；公开仓成功不能代替它。没有配置的自动重跑动作不索要相应写权限。
采用权限/配置读取与现有结果验证；需实测写路径时使用明确授权的 disposable 验证，不在普通需求预检中偷偷发起 PR 或 rerun。
缺权限、无有效检查来源或所需工作流无法触发时在领取前阻塞，显示配置缺项；权限/工作流变化使对应能力记录失效，重新核查后继续。

| 阶段 | 源码与检查绑定 |
|---|---|
| pre_merge | 明确使用 PR head 或 test-merge；记录目标 head/base、check 关联 SHA，以及验证实际检出的 SHA，不把三者混为一个字段 |
| post_merge | 明确实际 merged SHA 上的必需检查、触发器/受控验证命令及适用范围；独立于 pre_merge 定义 |

每个必需检查选择器包含来源类型（check-run/status/local）、受信发布者及必要的 workflow/job/event/分支约束；记录 run/suite/attempt 或相应状态身份。
同名不代表同一个逻辑检查；同一逻辑检查选择当前有效运行/重跑，旧失败不永久阻挡已有效重跑的成功，也不从冲突运行里随意挑选绿灯。
确认映射与被替代关系、完整分页及所有必需来源之后才判定；身份不明则等待/阻塞。API 的 `latest` 只是查询过滤条件，不独自证明此映射正确。
PR-only workflow 若不能产生所需 post_merge 结果，先配置对应触发或已授权的固定版本验证路径；不等待不存在的 CI，也不自动删除必需检查来绕过。
Checks API 与 commit statuses 各自按契约消费；无 status 的 combined `pending` 不阻挡已满足的 check-run，status-only CI 也不能被忽略。

### 11.2 合并判定与证据采信

合并事实先来自精确 PR 的可靠 `merged=true` / `merged_at` 或专用 `GET /pulls/{n}/merge` 成功响应；HTTP 错误、权限不足、字段缺失均作为未知，不能伪造已合并/未合并。
专用接口的 404 也结合仓库/PR 可访问性判断，不把资源不可访问当作确定未合并。`closed` 不能单独证明已合并。
**`merge_commit_sha` 非空不证明已合并**：开放 PR 可有 test merge SHA；确认合并事实后才按合并方式记录最终 SHA。
0a 只观察这些事实；V1 的自动合并才执行下列条件，避免将日常能力前置到内部里程碑。

自动合并前按 11.1 确认当前输入/策略、必需 CI、受信验证与适用 AC，必要 review 条件均满足且没有在途写入/修复/交接；缺失、失败、无依据的 skipped/N/A 不算通过。
以 expected head SHA 调用合并；head 或验证所依赖 base 变化则失效旧条件并重新取得证据。若用 test-merge 证据，所选仓库规则也需防止在未验证的新 base 上合并。
合并意图和身份先落库，超时或未知结果先查询事实；不得盲目换 PR/重新编码。实际合并后观察 11.1 的 post_merge 证据，不直接把 merge 当作业务 Done。
若平台不能执行仓库现有必需 review/分支策略，则在 11.1 就绪检查报告，不绕过保护或临时放宽策略。

API 语义依据：[PR 详情与 test merge SHA](https://docs.github.com/en/rest/pulls/pulls#get-a-pull-request)、[确认合并](https://docs.github.com/en/rest/pulls/pulls#check-if-a-pull-request-has-been-merged)、[Checks 查询](https://docs.github.com/en/rest/checks/runs#list-check-runs-for-a-git-reference)。S2 记录仅为特定实验，不替代这些当前规则。

## 12. 验证与修复预算

### 12.1 固定候选验证

完成声明后的候选 SHA 保全并结束 Run 后，平台在不可变候选检出上独立运行 custom gate 和适用 AC。
保存源码 SHA/tree、受信命令/配置/受保护入口身份、实际工具身份、步骤结果及原始日志引用。
本地固定候选检查使用普通进程，保留超时、输出限制和进程停止；不声称只读 namespace。验证前后校验源码和受信入口未被修改；Agent 改策略或将受保护检查替换成恒真不能自证通过。
通过才生成交接产物清单；缺失必需结果、退出码失败、身份不符或证据不可读均不能视作通过。
同一候选/步骤的重复请求幂等；执行结果未知先核查旧进程和输出，不并行重复执行。
0a 不跨批次缓存，不把编写期间自检当作平台最终验证；不实现收据式引文核验和 raw/view 双摘要协议。

### 12.2 代码修复

0a 每条根需求最多一次自动代码修复，适用于已分类且策略允许的 test/lint/build/AC 代码失败。
用根需求累计计数和唯一 repair ordinal 原子预留，并同事务建立修复 Run 意图；重复事件、重启和恢复不重复扣次、不重置次数。
修复上下文包含失败步骤、退出码、原文/日志引用、原候选和剩余 AC；摘要失败直接提供原始输出。
预留后启动结果未知先对账；原预留可恢复，不再加计数。安全/配置/授权失败不自动修复。
无需 RepairAttempt 六态状态机、CiRecovery、ReviewFix 或 formatter 新候选来源体系。

### 12.3 环境预检

在与 Agent 相同的执行身份和可信开发环境下检查：工具/依赖能力、可写构建和临时目录、Broker、所需服务连通性及磁盘。
项目接入时执行实际构建/测试 smoke；普通测试不依赖宿主专用验收回执。缺依赖、服务不可达等准备故障不启动编码模型、不消耗代码修复次数。
领取还核对 11.1 的仓库交付能力和有效配置；准备与网络重试有独立预算，磁盘/持久化故障阻止新领取及后续外部写入。恢复后从实际阶段接续。

### 12.4 Harness-Gate 接入边界

0a 使用 custom gate。Harness-Gate 接入计划见演进目录，不阻挡首条 PR。
本项目未来仍沿用 CRAP/覆盖率等质量要求，由 Harness-Gate 负责语义；本次文档收缩不修改任何仓库现有 CI 门禁。

## 13. Review

0a 展示 GitHub PR 链接和外部反馈；不自动生成 ReviewFix，不增加 reviewer Agent。
未来反馈去重与修复预算候选方案见 [自动闭环](docs/roadmap-specs/automatic-delivery.md)。

## 14. 持久化与事务

PostgreSQL 保存第 3 章记录和事件；不引入 Redis/MQ。只迁移已使用的记录，不预建未来表。
关键唯一键：一个全局队列占用者、每需求一个活跃编码 Run、Run/tool request、任务修复 ordinal、outbox action_key、repo+branch 的 PR 关联。
CAS 保护输入版本、待回答请求和状态更新；不以消息到达顺序推断执行完成。

以下操作各自原子落库：领取+Run 启动意图；结束意图互斥；修复预留+Run 意图；验证通过的产物+交接 outbox；外部结果+关联状态。
文件先完成写入与可读/摘要校验，再保存完整证据引用；数据库失败时不声称已封存、不继续外部动作，残留文件随后对账。材料压缩/删除和存储预留的崩溃对账按 10.4–10.6，不将已过期材料的旧引用标记为仍可下载。
文件系统、数据库和 GitHub 之间不承诺分布式事务；通过持久化意图、精确身份和重试对账恢复。

## 15. 模型与额度

首版固定一个 Codex Runtime，模型由 Repository 配置并记录到 Run；不实现动态路由、fallback 或金额预算平台。
区分输入、缓存输入、输出及模型调用用途；按协议累计值去重，未知值留空，不编造费用。
单 Run 进程寿命与可执行需求累计执行额度分别限制。获授权时冻结需求级 token/turn/模型工作时长上限；V1 同时冻结父组适用总上限。
每次启动或续轮前原子检查同一需求全部初始/恢复/修复 Run 的“已用＋在途预留＋本次预留”不超限，V1 同时检查组余额；执行中持续核算，达到限额停止并保全。
用量事件绑定 Run/turn 去重，结算时用实际用量替换预留；结果未知不提前归零释放。不能用新 Run 的空计数代替需求累计余额，也不把 token/turn 的本地上限宣传为精确金额计费上限。
恢复、重新拆分或新建关联修订不刷新原已用额度；显式增加额度保存新的授权及增加量，历史仍可查询。0a 不增加 12.2 的代码修复次数。
人工等待、暂停、平台 CI/网络等待单列为墙钟时长，不消耗模型工作时间；单 Run 安全寿命仍包含其等待，并可到期退出。业务问题的继续处理见 8.1。
0a 代码修复计数按 12.2，日常 V1 按日常契约第 4 节更新；准备/验证/交接重试按第 28 章更新。

## 16. API 与网络授权

当前 API 只服务第 1 章页面：仓库登记/就绪检查/列表、需求创建/列表/详情、Draft 更新、Ready/撤回、暂停/取消/恢复、Run 详情/事件、待办读取/回答、健康状态。
路由在实现时确定，不预建完整平台 API。控制命令绑定对象版本，请求幂等；不做 CLI/socket 和多客户端审批协议。
浏览器定时读取状态，不控制后台生命周期。

开发网络沿用部署环境的正常连接方式，0a 不实现静态强制白名单、逐 Run 网络授权或直接连接阻断证明。
`network_access` 仅记录所需服务和评审意图，供部署准备及连通性检查使用，不是防火墙规则。
环境管理员管理组织级网络策略；产品不改写全局 Codex 网络配置，不以缺少 allow/deny/bypass 证明阻塞开发。
GitHub 发布和签名服务可以独立限制自己的网络与权限，不把这些限制传播到普通开发命令。

## 17. 安全边界

0a localhost 绑定、拒绝非预期 Host/Origin、写请求防 CSRF；不得因没有远程访问就暴露无保护控制 API。
GitHub 凭据由独立操作服务持有，Agent 仅通过授权接口请求操作；凭据不注入开发环境。Broker 保留候选与恢复归属检查，普通本地 Git 不依赖只读 .git。
开发环境只运行本人可信代码，不承诺隔离恶意进程或多租户。密钥使用独立服务/账号/部署环境保护，不把环境变量过滤宣传为同 UID 文件读隔离。

正式 Gate 在独立验收服务中获取指定提交并使用获授权配置，签名密钥只留在该服务。开发测试结果只是开发证据，不获得签名权；不允许开发工作区自证正式 Gate 通过。
实际身份不匹配、策略撤销或秘密泄露失败时停止自动推进，保全必要脱敏证据并显示待办。
平台日志和 UI 不输出凭证；证据下载校验所属任务和受管路径。更完整的身份/隔离设计见演进目录。

## 18. 最小界面与阻塞卡

Angular + Material 复用 arc-admin 的视觉规范、设计 token 与共享样式，来源版本和适配边界见[UI 复用规范](docs/ui-design-system.md)。0a 页面随实现采用该基线；不另建一套设计系统，暗色切换和完整视觉回归体系仍可后置。
四页内容见第 1 章。表单标签、错误关联、键盘操作、可见焦点、按钮可访问名称和基本对比度随页面实现。

阻塞记录用固定字段回答六问：哪里失败、原因确认程度、已保存什么、平台已尝试什么、用户下一步、解除后从哪里恢复。
保留 failure_code、阶段、退出状态、原始输出引用、固定探针结果、缺失证据和解除条件；未知原因明确为未知。按 10.4–10.6 显示原始材料可用、已压缩、截断或已过期，及替代复盘记录；区分存储预算阻塞与环境故障。
只提供固定的回答、重新检查、暂停、恢复、取消、查看日志操作，执行前核对当前授权/版本；无需通用 action catalog 或 DiagnosticAttempt。
已知临时环境问题按有限探测恢复；未知问题给出可读事实，不要求模型编造根因或 shell 修复命令。
同一失败只更新一条待办；摘要不可用时显示脱敏原文。页面关闭或断线不影响执行。

## 19. 日志与指标

结构化日志携带 requirement/run/phase/action 身份、错误与决策原因；业务事件单独持久化，原始调试流限额保留。
必留指标：介入次数及原因、零介入率、到 PR 时长、人工等待时长、各阶段耗时、模型调用数、input/cached/output token、累计修复次数。
零介入率注明阶段与样本分母；等待不当成编码耗时，缓存输入不当作非缓存计费量。
补充全局/类别/需求的存储占用、预留、受保护字节、最老待清理项、回收量和清理失败原因；原始事件流及数据库占用纳入 10.6，不以“事件”名义无限保存完整重复输出。
用数据库查询和现有页面展示；不要求 OTel、Prometheus、Grafana、独立 Dashboard 或实验指标。

## 20. Rust Workspace

最多从三个 crate 起步：domain（模型与转换）、runtime（Codex 协议/进程）、server（Axum、SQLx、协调、GitHub、工作区）。
GitHub/CI/recovery/persistence 先作为模块；出现真实独立复用或构建边界后再拆 crate。
栈保持 Rust/Tokio/Axum/SQLx/PostgreSQL/reqwest/serde/tracing；版本在实现时锁定，不因文档拆分重做 S1～S5。

## 21. 部署

0a 单主机单控制面 + PostgreSQL，浏览器访问 localhost；ENV 管配置与代理，数据库/工作区/证据使用持久目录。
app-server 按第 8 章启动，可信开发环境和服务连通性按第 16 章验收。进程组监督需能处理后代进程；无法确认停止则不继续。
调度前和运行期间检查磁盘，按 10.6 计入预留和类别预算，保留控制面写入余量；持久化失败按 12.3 停止外部动作。容量配置与挂载校验是部署就绪条件。
升级前保存数据库及恢复资料；正式自动备份/隔离恢复演练在后续阶段完善。未验证灾难恢复前不承诺任意数据损坏可自动恢复。

## 22. 当前验收场景

每项只验证当前行为，不为后期框架建故障矩阵。模拟可确定的故障；真实模型/开发环境/GitHub 的关键边界用 disposable 仓库验证。
试用另记录至少 5 条不同真实需求的结果、复杂度说明、介入原因和消耗，用于改进；不设未经任务难度校准的 3/5 零介入发布门槛，也不将收集样本作为 M1–M3 的前置条件。

| ID | 场景与通过条件 | 权威规则 |
|---|---|---|
| A01 | 浏览器建需求、可解析 AC、Ready 后自动到 PR；必需证据可追溯，未实现的测试引用不被误判为必须已存在 | 4、7、11、12 |
| A02 | 握手/动态工具/事件/token、超时和人工回答正常；重复结束意图只接受一次，blocker 后不续轮；进程退出后隔夜回答仍有效且不重问 | 8、15 |
| A03 | 声明前重启保留 index/未提交/未跟踪工作；旧组仍活着或 PID 身份不明时不重跑；快照失败保留原件 | 6、10 |
| A04 | 声明后或验证中重启只恢复相应阶段；晚到消息不能推进新 Run，同步骤不并行重复验证 | 6、8、12 |
| A05 | push/PR 响应丢失、重复 outbox、取消/合并竞态能对账；取消可关闭关联开放 PR 后释放，不重复 PR、不强推；非空 test merge SHA 不冒充合并 | 4.4、11、14 |
| A06 | 修改受信命令/配置/入口、候选 SHA 改变、结果缺失均不能通过；Run 成功与 Gate 失败可独立查询 | 9、12、17 |
| A07 | 缺依赖/错误能力/只读构建目录在真实开发环境预检阻塞，模型调用为零；ENOSPC 停止后续外部写入 | 12、21 |
| A08 | 自动代码修复至多一次；重复失败/恢复/新 Run 不刷新需求级累计额度，已用和预留正确结算；安全失败不自动修复，原文可用于恢复 | 12、15、28 |
| A09 | 交接/CI 等待、人工阻塞及暂停仍占队列；回答不解除用户暂停；合并不冒充 Done；取消收尾未完成不释放 | 4、6、8.1 |
| A10 | 分类保留及跨重试归并生效；重复任务/重试的受管占用受配置预算约束，无法回收时在控制面余量耗尽前拒绝新工作；压缩/删除中断可恢复，活动消费者、唯一工作及 partial/pending 不丢失，复盘身份/过期状态准确，不调用模型 | 10.4–10.6、19、21 |
| A11 | 普通开发命令与真实 Runtime 测试可执行；GitHub/签名凭据不进入开发环境；服务连通性失败保留真实原因 | 16、17 |
| A12 | 四页和阻塞六问可用，键盘/错误提示正常；重复回答受控，日志脱敏，关闭页面不影响 Run | 16、18、19 |
| A13 | 第一条单仓通过后接入第二仓，领取前发现所选阶段的 GitHub 权限/检查能力缺口；精确路由且全局一次一项 | 6、11.1、23 |

## 23. 实施队列

Current: Phase 0a。队列以下列可演示结果推进，不并行开展多个实现需求；P0 依据 0.3 而非条目数。

### S. 已有 spike 输入

读取 [S1](spikes/s1/README.md)、[S2](spikes/s2/README.md)及[补测](spikes/s2/README_S2b.md)用于 Runtime/GitHub；[S5](spikes/s5/README.md)仅保留为旧网络策略实验记录，不定义当前部署要求。
[S3](spikes/s3/README.md)、[S4](spikes/s4/README.md)保留供后续接入，未验证项不宣称已支持。
这些实验不替代实现后的 A01～A13 验收。

### 0a 核心闭环

| 顺序 | 实施结果 | 规则来源 | 验收 |
|---|---|---|---|
| 1 | PostgreSQL 与最小需求/Revision/策略记录，Ready 评审入口 | 3、4、7、14 | A01 输入部分 |
| 2 | 单实例、严格全局领取、进程身份与启动恢复闸门 | 5、6、21 | A03、A09 |
| 3 | worktree、Broker、Codex 工具与停止/人工问题闭环 | 8、10、17 | A02、A03 |
| 4 | 真实开发环境预检、服务连通性与磁盘保护 | 12.3、16、21 | A07、A11 |
| 5 | 候选保全、受信验证、原文失败信息与一次代码修复 | 9、12、15、28 | A04、A06、A08 |
| 6 | GitHub App 条件 push、outbox、PR 关联与只读轮询 | 11、14 | A05、A09 |
| 7 | 四页、阻塞信息、存储预算与证据生命周期闭环；增量接入前序生产者 | 10.4–10.6、18、19、21 | A10、A12 |

先用一个可信仓库完成第一条 A01，立即开始有限日常试用；不把第二仓或后期阶段作为首条 PR 的前置条件。
实现中按依赖补齐上述结果，不要求等全部页面美化后才做首次贯通。

### 0a 尾部：多仓登记

首条 A01 通过后，增加第二仓登记/选择与 App 仓库身份路由，执行 A13。
仍不增加并行、公平调度、远程 Worker 或多安装平台。完成第 24 章后发布 0a。

### 后续阶段

已确认的下一步是[日常 V1 契约](docs/daily-use-v1.md)第 7 节 M1–M3：需求拆分与整组队列、手机接续、自动修复合并及整体验收。
这些产品能力不再作为可选路线图；实施前只细化具体接口和场景，不重新讨论是否需要。复杂框架仍按演进目录的触发条件评估。

## 24. Phase 0a Definition of Done

本章只索引第 22 章，不另写状态、授权或恢复规则。第一条 A01 是试用里程碑，完整 0a 发布需以下全部通过。

2026-09-20 验收索引：[GH-24 真实 A01](docs/quality/gh24/README.md)、
[A01–A12 回归映射](docs/single-repository-acceptance.md)、[GH-25 第二私仓 A13](docs/quality/gh25/README.md)。
A13 已记录首仓占用时零执行、精确合并后串行领取、第二仓真实验证与 PR/CI；
原累计预算和共享存储身份保留。以下发布清单不由开发自测自动勾选，精确提交双 Gate 仍待独立验收。

- [ ] A01：真实需求到 PR，必需证据和评审后介入原因可追溯。
- [ ] A02：Runtime、动态工具、回答、token 与结束互斥。
- [ ] A03、A04：旧组静止、工作保全和按阶段恢复。
- [ ] A05：外部动作幂等、未知结果和取消对账。
- [ ] A06：候选与受信配置身份、缺失证据拒绝通过。
- [ ] A07：真实预检和磁盘故障保护。
- [ ] A08：有限修复与累计预算。
- [ ] A09：严格串行和外部事实/业务状态分离。
- [ ] A10：分类保留、重试归并、容量准入及中断回收；重复运行容量验收和唯一工作保护。
- [ ] A11：普通命令/真实 Runtime 测试可执行，服务连通性检查与凭据独立托管。
- [ ] A12：最小页面、基本可访问性、阻塞六问和日志指标。
- [ ] A13：首条跑通后完成多仓登记，仍严格全局串行。

0a 仅为内部里程碑，不等于日常 V1 完成；日常发布还需[日常 V1 验收 B01–B08](docs/daily-use-v1.md#8-v1-验收)通过。后期测试矩阵、诊断 Agent、完整租约、缓存复用、模型上下文压缩、视觉体系均不进入本清单；第 10 章的证据压缩/回收属于当前 A10。

## 25. 规则索引

| 主题 | 唯一行为定义 |
|---|---|
| 真相与 Gate 边界 | docs/architecture-boundaries.md |
| 授权策略归属 | 3.2 |
| 状态、输入修改、暂停/取消 | 4 |
| 全局顺序与恢复闸门 | 6 |
| Runtime 请求/结束与持久化问题 | 8 |
| 工作保全/恢复/清理 | 10 |
| 发布、仓库交付契约、合并与外部观察 | 11 |
| 验证/代码修复/预检 | 12 |
| 原子写入 | 14 |
| 跨 Run 执行额度 | 15 |
| 网络授权 | 16 |
| 失败动作与重试 | 28 |

## 26. 当前风险

0a 的可信代码边界和单机持久存储是明确限制，不宣传为多租户或不可信执行平台。
恢复和开发环境检查可能发现锁定版本的实际差异；保存进度并修复部署契约，不新增逐测试执行通道。
记录真实介入原因后决定投资方向，不承诺收缩规格必然提前数周或保持某个零介入率。

## 27. 日常版本与后期方案入口

[日常 V1](docs/daily-use-v1.md)是正式交付目标；[可选加固](docs/roadmap-specs/remote-and-hardening.md)、[自动交付优化](docs/roadmap-specs/automatic-delivery.md)、[按需扩展](docs/roadmap-specs/optional-platform.md)才是后期候选。
演进目录文件中的候选设计没有当前约束效力，不能据此增加 0a 数据表、端点、验收或依赖。

## 28. 当前错误分类与重试

失败记录至少包含 code、phase、原始错误/退出码、证据引用、已尝试次数和 next_attempt_at。
各阶段固定重试总上限：准备、验证基础设施、交接、清理各最多 2 次自动重试（初次执行之外），默认退避 30 秒/120 秒。
环境探测最多 2 次、10 分钟 deadline；超限生成待办。用户明确恢复可授权一组新的基础设施重试，保留全部历史次数和原因；不能增加 12.2 的代码修复上限。
正常只读轮询不是重试编码，查询失败逐步退避至 5 分钟，显示 stale；收到取消/撤销后不推进新写动作。

| code | 当前处理 |
|---|---|
| preparation_dependency_missing | 不调用模型；固定探测或待办 |
| preparation_capability_mismatch | 阻塞，纠正工具/环境后预检 |
| preparation_path_unwritable | 阻塞，权限恢复后预检 |
| storage_unavailable | 停止新领取/外部写，保全原目录并对账 |
| network_scope_unavailable | 显示部署授权缺口，按第 16 章处理 |
| runtime_start_failed | 核对旧组与启动意图，再有限恢复 |
| runtime_timeout | 停止、保全；按预算和阶段恢复 |
| runtime_protocol_error | 阻塞，保留脱敏协议诊断 |
| user_input_pending | 按 8.1 保留业务问题；进程可超时，有效答案可恢复 |
| validation_code_failed | 仅按 12.2 申请一次代码修复 |
| validation_infrastructure_failed | 只重试验证，不重启编码 |
| policy_identity_mismatch | 停止自动推进，需明确处理 |
| authorization_revoked | 停止新动作、保全与核查 |
| github_transient_or_unknown | 先查询事实，再有限重试交接 |
| github_identity_conflict | 阻塞，显示期望/实际身份 |
| cleanup_failed | 保留失败记录，有限重试及待办 |
| budget_exhausted | 停止模型调用并保全 |
| unknown | 保留原始错误与缺失信息，不猜测自动修复 |

HTTP 状态、工具原生错误保留，不提前枚举未启用功能的全部错误码。

## 开发环境补充（2026-09-15）

本仓库开发门禁立即使用 Harness-Gate，CRAP 上限 10、覆盖率至少 80%，独立于未来平台的 custom／Harness-Gate 分期。发布二进制版本与摘要由 `harness-gate-version.lock` 固定；候选测量插件与完整本机证据见 `.harness-gate/QUALITY.md`。版本升级须重新验收，不复用 S4 历史结论。

Codex 精确版本由 `codex-version.lock` 固定到 0.154.0；schema/codegen 由同版本生成，升级须通过兼容性检查。Rust 构建显式使用工作区内或平台专用 `CARGO_TARGET_DIR`，不继承用户级共享 target 目录。

后续手机阶段采用平台用户名、密码登录，账号、会话与验收要求以[日常 V1 契约第 6.1 节](docs/daily-use-v1.md#61-用户名密码登录)为准，不依赖 Cloudflare Access。既有 Tunnel 可继续用于网络转发，重新验收 HTTPS、源站隔离和应用认证；通知使用 Bark JSON POST，device key 不进入 URL、仓库或 Agent 可读凭据。通知器的网络和凭据隔离独立于 Agent。备份阶段保留离机受限加密副本，并做禁用外部写的恢复演练。这些部署选择不提前扩大 Phase 0a。

## 实践修订（2026-09-16）

本次磁盘事故暴露了“编译副本、原始测量、恢复资料全部长期保留”和失败重试残留的持续增长。规范已在 10.4–10.6 修订，A10 与实施队列同步引用。产品实现影响与已开发需求核对见 [证据生命周期需求影响](docs/evidence-lifecycle-impact.md)；该文记录现状和分工，不另定义保留规则。PR #40 的宿主运维脚本是实践验证，不代表 Rust 产品已完成 A10。
